server:
  port: 8082

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://postgres:5432/company_db?currentSchema=user_data
    username: postgres
    password: qzwxecrv
    driver-class-name: org.postgresql.Driver
    initialization-mode: always
    schema: classpath:schema.sql
    data: classpath:data.sql

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true