spring:
  application:
    name: gateway-service
  config:
    import: optional:configserver:http://config-service:8888
  cloud:
    config:
      enabled: true
      fail-fast: false
      profile: docker